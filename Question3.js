// Calculate cylinder volume

let diameter = 10;
let height = 5;
let radius = diameter/2;
let volume = Math.PI*(radius**2)*height;

console.log (`The volume of the cylinder is ${volume} meters squared`);